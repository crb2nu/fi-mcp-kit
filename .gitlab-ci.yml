# Go CI - Uses shared templates from platform/gitops
include:
  - project: 'platform/gitops'
    file: '/ci/templates/go.yml'

stages: [lint, test, build]

lint:
  extends: .go-lint

test:
  extends: .go-test

build:cli:
  extends: .go-build
  script:
    - CGO_ENABLED=0 go build -ldflags="-s -w" -o fi-mcp ./cmd/fi-mcp
  artifacts:
    paths:
      - fi-mcp
    expire_in: 1 week
